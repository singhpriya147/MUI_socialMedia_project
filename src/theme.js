import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#63317C',
      light: 'skyblue',
    },
    secondary: {
      main: '#A52A2A',
    },
    othercol: {
      main: '#999',
    },
  },
});
